
document.addEventListener("DOMContentLoaded", function () {

    const botonDerecho = document.querySelector(".boton-derecho");
    const campoTexto = document.querySelector(".campo-texto");
  
    // Ejemplo opcional: si querés evitar que navegue si el input está vacío
    // botonDerecho.closest("a").addEventListener("click", function (e) {
    //   if (campoTexto.value.trim() === "") {
    //     e.preventDefault();
    //     alert("Escribí algo antes de crear la cuenta");
    //   }
    // });
  
  });
  
  